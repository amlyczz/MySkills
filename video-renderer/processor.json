{
  "name": "VideoRenderer",
  "description": "使用 Remotion 渲染视频帧序列 → video.mp4",
  "version": "1.0",
  "input": {
    "contract": "pipeline-contracts/py/video_config#VideoConfig",
    "description": "渲染配置（场景/布局/动效/过渡）"
  },
  "input_extras": [
    {
      "contract": "pipeline-contracts/py/timeline#TimelineModel",
      "description": "时间线配置"
    }
  ],
  "output": {
    "contract": "file",
    "description": "video.mp4"
  },
  "config": {
    "properties": {
      "crf": { "type": "integer", "default": 18 },
      "codec": { "type": "string", "default": "h264" }
    }
  },
  "exec": {
    "type": "command",
    "command": "cd video-renderer/remotion && npx remotion render VideoComposer <output>/video.mp4 --props=<video_config> --codec h264 --crf 18"
  }
}
