{
  "name": "ScriptTimelineComposer",
  "description": "AI Agent 场景编排。为每段口播逐一决策 13 个视觉维度，输出 timeline.json + video_config.json",
  "version": "2.0",
  "input": {
    "contract": "pipeline_contracts.content#ContentModel",
    "description": "ReproAnalyzer 产出的完整 ContentModel（含素材绑定的口播脚本）"
  },
  "output": {
    "contract": "pipeline_contracts.timeline#TimelineModel",
    "description": "timeline.json + video_config.json + timeline.srt + timeline.bgm_curve.json"
  },
  "exec": {
    "type": "agent",
    "description": "AI Agent 主导语义推理，timeline_composer.py 作为确定性工具补充"
  }
}
